# Linux / Docker production: app deps + CPU Torch + SpeechBrain LID.
# Used by Dockerfile. Local Windows: use requirements.txt + optional requirements-ivr-lid.txt.
--extra-index-url https://download.pytorch.org/whl/cpu
-r requirements.txt
torch==2.9.1+cpu
torchaudio==2.9.1+cpu
speechbrain>=1.0.0,<2
huggingface_hub>=0.23.0
scipy>=1.11.0
