# Optional / local IVR language-ID stack.
# Docker / requirements-linux.txt install this automatically.
# Local Windows:
#   .\venv\Scripts\python.exe -m pip install -r requirements-ivr-lid.txt
#
# CPU Torch wheels (2.9.1+ is what the pytorch.org cpu index currently serves):
#   --extra-index-url https://download.pytorch.org/whl/cpu

torch>=2.9.0
torchaudio>=2.9.0
speechbrain>=1.0.0,<2
huggingface_hub>=0.23.0
scipy>=1.11.0
